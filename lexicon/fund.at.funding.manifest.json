{
  "lexicon": 1,
  "id": "fund.at.funding.manifest",
  "description": "Structured funding metadata: payment channels and plans. ATProto-native equivalent of funding.json (fundingjson.org) with DID-signed provenance.",
  "defs": {
    "main": {
      "type": "record",
      "description": "Machine-readable funding manifest. Declares how people can support this entity — payment channels and optional plans with amounts. Channel and plan types are defined in fund.at.funding.defs for reuse.",
      "key": "literal:self",
      "record": {
        "type": "object",
        "required": ["channels"],
        "properties": {
          "channels": {
            "type": "array",
            "description": "Payment channels where contributions can be made.",
            "minItems": 1,
            "maxLength": 10,
            "items": {
              "type": "ref",
              "ref": "fund.at.funding.defs#channel"
            }
          },
          "plans": {
            "type": "array",
            "description": "Funding tiers or plans with suggested amounts.",
            "maxLength": 10,
            "items": {
              "type": "ref",
              "ref": "fund.at.funding.defs#plan"
            }
          },
          "createdAt": {
            "type": "string",
            "format": "datetime",
            "description": "When this manifest was created or last updated."
          }
        }
      }
    }
  }
}
