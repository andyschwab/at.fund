/** Public Bluesky API base URL (unauthenticated). */
export const PUBLIC_API = 'https://public.api.bsky.app'

/** Maximum actors per `app.bsky.actor.getProfiles` request. */
export const PROFILE_BATCH = 25

/** Maximum URIs per `app.bsky.feed.getFeedGenerators` request. */
export const FEED_BATCH = 25
